import React from 'react'
import Button from './Button'

// const Navbar = (props) => {
const Navbar = () => {
    return (
        <div>
            <div>
                navbar
            </div>
            <div>
               {/* <Button count={props.count}/> */}
               <Button/>
            </div>
        </div>
    )
}

export default Navbar
